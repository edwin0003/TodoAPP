
<div class="container py-5 h-100" style="text-align: center;">
    <div class="container" style="width:80%, height: 70%">
        <h1>TODO LIST</h1>
        <table class="table  table-dark">
            <thead>
                <tr class="table-danger">
                    <th scope="col">N°</th>
                    <th scope="col">Task</th>
                    <th scope="col">Data Time</th>
                    <th scope="col">Action</th>
                </tr>
            </thead>
            {{#each listTasks}}
            <tbody>
                <tr>
                    <th scope="row">{{@index}}</th>
                    <td>{{task}}</td>
                    <td>{{createAt}}</td>
                    <td>
                        <div class="d-flex inline-flex justify-content-between">
                            <div>
                                <form action="/" method="post">
                                    <input type="hidden" value="{{id}}" name="id">
                                    <button type="subtmit" class="btn btn-danger"><i class="bi bi-trash"></i></button>
                                </form>
                            </div>
                            <div>
                                <a href="edit/{{id}}" class="btn btn-primary"><i class="bi bi-pen"></i></a>
                            </div>
                        </div>
                    </td>
                </tr>
            </tbody>
    </div>
    {{/each}}
    </table>
</div>
</div>